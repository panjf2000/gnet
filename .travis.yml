language: go
env:
  - GO111MODULE=on
os:
  - linux
  - osx
  - windows
go:
  - "1.11.x"
  - "1.12.x"
  - "1.13.x"

before_install:
  - go get -t -v ./...

script:
  - go test -coverprofile=coverage.txt -covermode=count -v -timeout 30s

after_success:
  - bash <(curl -s https://codecov.io/bash)